import re

with open('src/features/admin/question-builder/components/QuestionBuilder.tsx', 'r') as f:
    content = f.read()

# I will use Python to make surgical replacements using regex or string replace.
# First, let's prepare the updated code for QuestionBuilder.tsx
